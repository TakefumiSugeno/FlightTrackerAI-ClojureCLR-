# 批判的レビュー記録: 新規フライト登録における入力項目の完全統一

- **対象タスク計画書**: `doc/tasks/20260913-010300_unify_flight_task_registration_inputs/tasks.md`
- **合意レベル**: L2 (中規模 - UI/UX画面項目統一・フロント/バックエンド連携)
- **レビュー実施日時**: 2026-09-13 01:05
- **レビュアー**: サブエージェント（「ユーザー」「SE/PG」ロール）

---

## 1. 【ユーザー（エンドユーザー・旅行計画者）ロール】のレビュー結果

| 重要度 | 指摘事項 | 指摘理由 | 推奨対応策 |
| :---: | :--- | :--- | :--- |
| **🚨 Critical** | **Webhook通知チェックボックスの有名無実化（期待の裏切り）** | ユーザーが「このタスクは通知不要」と判断してチェックを外しても、バックエンド側でタスク個別URLが空だと無条件で全体設定のWebhookに通知が送信されてしまう。UIの選択肢がダミーになっており、深夜通知等で重大なストレスを招く。 | チェックOFF時は通知を完全に無効化（スキップ）する制御を実装する。また、タスク個別の通知先URLも任意で指定できるようにする。 |
| **🚨 Critical** | **AI解析モーダル展開時の「条件リセット（乗継・タイトル）」による手戻り** | 「直行便のみ」とAIに指定したにもかかわらず、モーダルが開くと「乗継制限なし」に勝手にリセットされてしまい、手動で再選択を強いられる。 | AI解析結果の `:MaxStops` および推定 `:Title` を100%確実にモーダル初期値へ反映する。 |
| **⚠️ Major** | **「同一タブモーダル」vs「新規別タブ画面」のUX二重分断** | AI解析時に新規タブが開き、登録後にそのタブがダッシュボードになるため、同一画面が2つ開いてユーザーが混乱する。キャンセル時も同様。 | スタンドアロン画面のキャンセル時は別タブであれば `window.close()` を優先する。将来的にはモーダル内への直接反映へ統一。 |
| **⚠️ Major** | **タスク名（Title）の初期補完とAI生成の期待値ギャップ** | 複数タスクを監視する旅行者にとってタスク名は最重要。単なる「HND ➔ CDG」だけでなく、AIプロンプトから目的（例:「GWパリ旅行」）を抽出・補完して入力済みにする。 | AI解析プロンプトからのタイトル抽出、またはフォールバックとして `[出発地] ➔ [目的地]` を初期入力値として設定。 |
| **⚠️ Major** | **全体設定（巡回間隔等）の動的反映漏れと日付バリデーション** | スタンドアロン画面で巡回間隔が「全体設定に従う (現在 12h)」とハードコードされており、全体設定と乖離する。過去日付の選択ガードも必要。 | システム設定値を参照して動的表示する。日付入力に `min` 属性を付与して過去日選択を抑止。 |
| **💡 Minor** | **UI文言・プレースホルダーの表記ゆれ** | 「構造化メモ / 要望・制約（任意）」 vs 「メモ・要望」など、画面間で言葉遣いが異なることによる認知負荷。 | 完全に同一の文言・ヘルプテキスト・プレースホルダーに統一する。 |

---

## 2. 【SE / PG（設計・開発技術者）ロール】のレビュー結果

| 重要度 | 指摘事項 | 指摘理由 | 推奨対応策 |
| :---: | :--- | :--- | :--- |
| **🚨 Critical** | **Webhook通知フラグとドメインモデル・DB・巡回ロジックの構造的断絶** | `flight_tasks` テーブルおよび `FlightTask` に `use-default-webhook` フラグが存在せず、フォーム送信でチェックOFFにしても保存・判定されない。 | チェックOFFの場合は `:notification-webhook-url` に `"DISABLED"` を格納し、`notification.clj` で `(not= (:notification-webhook-url task-item) "DISABLED")` と判定して通知をスキップする堅牢かつ安全な設計とする。 |
| **🚨 Critical** | **フォームレンダリングおよびPOSTハンドラーの二重管理（コピペ実装）** | 今回の不整合の根本原因は、モーダルとスタンドアロン画面で12項目のフォームHTMLおよびPOST処理を別々にベタ書きしていたことにある。 | **ビュー層**: `render-task-form-fields` を単一の共通部品関数として切り出し、双方から呼び出す。<br>**コントローラ層**: 入力パース・バリデーション・TaskItem生成を `parse-task-form` に一元化。 |
| **⚠️ Major** | **スタンドアロン画面におけるバリデーションエラー時の画面崩壊** | `POST /api/tasks/standalone` でエラーが発生した場合、白画面に `<p class='text-rose-400'>エラー文</p>` だけが返り、入力値も消滅してブラウザ戻るボタンが必要になる。 | エラー発生時に入力値を維持したまま、`render-standalone-new-task-page` をエラーメッセージ付きで再レンダリングする。 |
| **⚠️ Major** | **パラメータバインディングのキー名不一致と型安全性の欠如** | `maxPriceJpy` vs `targetPriceJpy`、`notes` vs `userNotes` などパラメータ名が不統一。`maxStops` のパースもコントローラ側で表記揺れに弱い `case` 文になっている。 | パラメータ正規化ヘルパーを作成し、`domain/max-stops-from-string` を使用して安全にパースする。 |
| **⚠️ Major** | **AI解析〜フロントエンド契約におけるパラメータ欠落** | `ai-client.clj` でタイトルをパースしておらず、`layout.clj` の `parseWithAI()` でも URL パラメータに `title` を含めていない。 | AI Client ➔ Controller ➔ JS URLSearchParams ➔ Standalone / Modal のパイプライン全体で `title` と `maxStops` を確実に伝達する。 |
| **💡 Minor** | **チェックボックス値の解釈の堅牢性** | HTMLチェックボックス未チェック時はキー自体が送信されない（`nil`）。厳密な文字列一致ではなく真偽値判定ヘルパーを導入する。 | `(boolean (or (= v "true") (= v "on") (= v "1")))` による寛容な判定を行う。 |

---

## 3. レビュー指摘への対応方針（タスク計画への反映）

上記指摘をすべて受け入れ、`tasks.md` を以下の通り改訂した：

1. **フォーム共通部品化**: `modals.clj` に `render-task-form-fields` を新設し、手動モーダルとスタンドアロン画面で同一のフォームロジックを共有する（コピペ根絶）。
2. **Webhook通知無効化制御**: チェックOFF時に `"DISABLED"` を記録し、`notification.clj` で確実に通知をスキップする。
3. **AIパラメータの完全連携**: `ai-client.clj`, `layout.clj`, `api_controller.clj` で `title`, `maxStops`, `notes` を漏れなく受け渡す。
4. **スタンドアロン画面のエラーリカバリ**: バリデーションエラー時に入力値を保持して再描画する。
5. **全体設定連動と日付最小値ガード**: 巡回間隔の動的反映と本日以降日付の選択ガードを導入。

---

## 4. 【Step 2: 仕様・設計・UIモック】のサブエージェントレビュー結果

- **レビュー日時**: 2026-09-13 01:08
- **レビュアー**: サブエージェント（Design & Mock Reviewer）
- **総合判定**: **【条件付き合意推奨 (LGTM with Minor Recommendations)】**

### 主な評価と対応結果

1. **全12項目の完全網羅**:
   - 手動登録モーダル (`index.html`) およびスタンドアロン画面 (`standalone_new_task.html`) の双方が全12項目を網羅していることを確認。
2. **モック微調整指摘への対応**:
   - `index.html` 側の `<option value="1">` を `value="OneStop"` に統一。
   - `index.html` の日付選択にも `min="2026-09-13"` を追加し、過去日ガードを完全統一。
3. **ドキュメント側の完全性担保**:
   - `design_detail.md` の 5.2 節（DDL）に `is_headless INTEGER NOT NULL DEFAULT 1` を追記。
   - `design_detail.md` 第4章の対応表に `showBrowser` / `is_headless` の対応行を追記。
   - `spec.md` 3.1 節に `ShowBrowser` と `UseDefaultWebhook` の仕様を追記。
4. **実装時の注意事項確認**:
   - `render-task-form-fields` は `<form>` タグを含まない純粋関数として切り出すこと。
   - タスク編集時（`is-edit` モード）において `webhook_url` が `"DISABLED"` の場合にチェックボックスを OFF で初期表示する逆変換処理を組み込むこと。

---

## 5. 【Step 3: 実装・検証】のサブエージェントレビュー結果および対応記録

- **レビュー日時**: 2026-09-13 01:19
- **レビュアー**: サブエージェント（Architecture & Code Quality Reviewer）
- **総合判定**: **【条件付き合意推奨 (LGTM with Recommendations)】**

### 主な評価

1. **DRY原則の徹底**:
   - `modals.clj` に `render-task-form-fields` を新設し、全12項目の入力フィールド生成を一本化。コピペ負債を根絶。
2. **Webhook無効化（DISABLEDセンチネル）**:
   - DBスキーマ変更なしで、未チェック時に `"DISABLED"` を記録。`notification.clj` で確実に通知スキップを実行。
3. **スタンドアロン画面のエラーリカバリ**:
   - バリデーションエラー時に白画面化せず、入力値を保持して完全なページHTML（HTTP 400）を再レンダリング。
4. **AI解析パイプラインの完全連携**:
   - `ai_client.clj` ➔ `api_controller.clj` ➔ `layout.clj` ➔ `modals.clj` の全経路で `title`, `maxStops`, `userNotes`, `targetPriceJpy` を漏れなく伝達。
5. **テスト網羅性**:
   - 全22スイート、518アサーション通過 (fail: 0, error: 0)、Overall カバレッジ **94.9%** を達成。

### レビュー提案事項への対応結果

1. **【提案1: 既存タスク編集時（`POST /api/tasks/:id`）におけるチェックボックスOFF判定の堅牢化】**
   - **内容**: HTML仕様上、チェックボックス未チェック時はキー自体が送信されないため、タスク編集送信（`is-full-form`）時に未送信であればユーザーがチェックを外したと判断して `"DISABLED"` および `is-headless: true` へ確実に更新するロジックを実装。
   - **対応結果**: `api_controller.clj` に反映し、単体テスト `test-update-task-post` にてチェック外し時の `"DISABLED"` 保存および `is-headless: false` 更新を検証・パス。
2. **【提案2: システム全体設定（巡回間隔）の動的反映】**
   - **内容**: `render-task-modal` および `render-standalone-new-task-page` に `default-interval` オプションを渡せるシグネチャを拡張し、コントローラ（`server.clj`, `api_controller.clj`）からシステム設定の `default-check-interval-hours` を渡して「全体設定に従う (現在 6h/12h)」の動的表示を完全連動。
   - **対応結果**: `modals.clj`, `server.clj`, `api_controller.clj` を更新し、全テストパスを確認。

